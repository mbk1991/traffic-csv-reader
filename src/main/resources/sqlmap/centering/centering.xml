<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.nextcore.collectormodule.centering.mapper.CenteringMapper">

<select id="selectSKey" parameterType="java.lang.String" resultType="java.util.Map">
    select skey, fltstatus from nc_r_sensor_t_1 where sensor_id = #{sensorId}

</select>
<insert id="insert" parameterType="java.util.Map">
    insert into nc_p_day_hopp_t(skey, getDate, hopp_status, left_status, right_status, center_status, image_path, image_name, image_type, image_flag)
    values(#{sKey}, #{getDate}, #{hopperingStatus}, #{leftStatus}, #{rightStatus}, #{centerStatus}, #{imagePath}, #{imageName}, #{imageType}, #{imageFlag})
</insert>

</mapper>
